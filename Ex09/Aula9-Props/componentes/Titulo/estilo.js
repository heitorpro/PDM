import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({
  box: {
    backgroundColor: '#000',
    alignItems: 'center',
    padding: 10,
    paddingTop: 20,
    marginTop:20,
  },
  texto: {
    fontSize: 28,
    color: 'red'
  }
});

export default estilo;